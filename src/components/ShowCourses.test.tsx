export {};
/* import React from "react";
import { fireEvent, render, screen } from "@testing-library/react";
import { ShowCourses } from "./ShowCourses";
import userEvent from "@testing-library/user-event";
import { Plan } from "../interfaces/plan";
import { Year } from "../interfaces/year";
import { Semester } from "../interfaces/semester";
import { Course } from "../interfaces/course";
import App from "../App";
// either define a sample value of a semester defined in component
// or test state by rendering app
describe("ShowCourses Component tests", () => {
    beforeEach(() => {
        render(<App />);
    });

    test("renders the word Course somewhere", () => {
        render(<ShowSemesters catalog={catalog} />);
        const linkElement = screen.getByText(/Fall/i);
        expect(linkElement).toBeInTheDocument();
    });
});
*/
