export {};
/*import React from "react";
import { fireEvent, render, screen } from "@testing-library/react";
import { ShowSemesters } from "./ShowSemesters";
import userEvent from "@testing-library/user-event";

test("renders the word Course somewhere", () => {
    render(<ShowSemesters catalog={catalog} />);
    const linkElement = screen.getByText(/Fall/i);
    expect(linkElement).toBeInTheDocument();
});


describe("ShowSemesters Component tests", () => {
    beforeEach(() => {
        render(<ShowSemesters />);
    });
});
*/
